# 4D水中壁紙 使用ガイド

## クイックスタート

### 1. ローカルで開く

最も簡単な方法は、`index.html` をダブルクリックしてブラウザで開くことです。

### 2. ローカルサーバーで実行（推奨）

より良いパフォーマンスのためには、ローカルサーバーを使用してください：

**Python 3を使用:**
```bash
cd Dxkabegami
python3 -m http.server 8000
```

その後、ブラウザで `http://localhost:8000` を開きます。

**Node.jsを使用:**
```bash
npx http-server
```

### 3. モバイルデバイスで使用

1. ウェブサーバーでホストする
2. モバイルブラウザでアクセス
3. 「ホーム画面に追加」でアプリのように使用可能

## 操作方法

### デスクトップ
- **クリック**: 水面をクリックして波紋と水しぶきを作成

### モバイル
- **タップ**: 画面をタップして水面に触れる
- **マルチタッチ**: 複数の指でタップして複数の波紋を作成

## 機能説明

### 水面エフェクト
- リアルタイムで波が動く
- 光が水中に差し込む
- タッチすると波紋が広がる

### 魚の動き
- 15匹の魚が自然に泳ぐ
- 尾びれが揺れる
- ランダムに方向転換
- 深度による遠近感

### インタラクション
- クリック/タップで波紋を作成
- 水しぶきが跳ねる
- 最大10個の波紋を同時表示
- パーティクルエフェクト

## カスタマイズ

`wallpaper.js` の `CONFIG` オブジェクトで設定を変更できます：

```javascript
const CONFIG = {
    fishCount: 15,        // 魚の数
    particleCount: 300,   // パーティクルの数
    rippleDuration: 2000, // 波紋の持続時間（ミリ秒）
    maxRipples: 10        // 最大波紋数
};
```

## トラブルシューティング

### 画面が真っ暗
- JavaScript が有効になっているか確認
- コンソールにエラーがないか確認
- ブラウザのキャッシュをクリア

### 動作が遅い
- ブラウザを最新版に更新
- 他のタブを閉じる
- `CONFIG.fishCount` を減らす

### タッチが反応しない
- タッチイベントが有効か確認
- ブラウザの互換性を確認

## ブラウザ要件

- **Chrome/Edge**: ✅ 推奨
- **Firefox**: ✅ 動作確認済み
- **Safari**: ✅ 動作確認済み
- **モバイルブラウザ**: ✅ 最適化済み

Canvas 2D APIをサポートする全てのモダンブラウザで動作します。

## パフォーマンス

- **FPS**: 60 FPS ターゲット
- **CPU使用率**: 低～中程度
- **メモリ**: 約50-100MB
- **バッテリー**: モバイルで最適化済み

## ライセンス

MIT License - 自由に使用、改変、配布できます。
